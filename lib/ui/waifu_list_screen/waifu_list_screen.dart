import 'package:flutter/material.dart';

class WaifuListScreen extends StatelessWidget {
  const WaifuListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
